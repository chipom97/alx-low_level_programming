#include "main.h"

/**
 * _strncat - concatenate strings
 * @dest: destination string
 * @src: source string
 * @n: integer
 * Return: string
 */

char *_strncat(char *dest, char *src, int n)
{
int index = 0;
int copy = 0;
while (dest[index] != '\0')
{
index++;
}
while (scr[copy] != '\0' && copy < n)
{
dest[index] = src[copy];
index++;
copy++;
}
dest[index] = '\0';
return (dest);
}
