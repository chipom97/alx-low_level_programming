#include <stdio.h>
#include "main.h"

/**
 * puts_half - print half string
 * @str: string
 * Return: half string
 */

void puts_half(char *str)
{
int len = 0;
while (str[len] != '\0')
{
len++;
str++;
}
printf("%i", len);
putchar('\n');
}
