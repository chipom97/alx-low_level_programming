#include "main.h"
#include <stdio.h>

/**
 * _strlen - string length
 * @s: string
 * Return: integer
 */

int _strlen(char *s)
{
int count;
count = 0;
while (*s != '\0')
{
count++;
s++;
}
return count;
}
